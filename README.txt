dependencies:
    cryptography
    customtkinter
    tkinter
    hashlib
    os

all can be installed via pip

Syntax:
python3 main.py

This application can only run on Windows operating system, and can be called
from the command line or with the executable file provided, as long as the 
executable remains in the same directory as the source code.